<%- include("partials/header") %>

    <h1>publicaciones</h1>
    <div>
        <% for(let i=0; i < posts.length; i++) {%>
            <article>
                <h2><%= posts[i].title %></h2>
                <p><%= posts[i].body%></p>
            </article>
            <%}%>
    </div>
    <%- include("partials/footer") %>